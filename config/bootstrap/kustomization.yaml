resources:
- bootstrap.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: bootstrap-config
  namespace: flomesh
  files:
  - bootstrap_config.yaml
- name: repo-init-scripts
  namespace: flomesh
  files:
  - scripts.tar.gz

