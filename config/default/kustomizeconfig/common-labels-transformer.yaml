apiVersion: builtin
kind: LabelTransformer
metadata:
  name: custom-common-labels
labels:
  app.kubernetes.io/name: flomesh-service-mesh
  app.kubernetes.io/version: ${FSM_VERSION}
fieldSpecs:
- path: metadata/labels
  create: true