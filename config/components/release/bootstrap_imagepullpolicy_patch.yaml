apiVersion: apps/v1
kind: Deployment
metadata:
  name: bootstrap
  namespace: flomesh
spec:
  template:
    spec:
      containers:
      - name: repo
        imagePullPolicy: IfNotPresent
      - name: bootstrap
        imagePullPolicy: IfNotPresent