apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.tg.repo.name }}
  namespace: {{ include "tg.namespace" . }}
  labels:
    {{- include "traffic-guru.repo.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.tg.repo.replicaCount }}
  selector:
    matchLabels:
      {{- include "traffic-guru.repo.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "traffic-guru.repo.labels" . | nindent 8 }}
        {{- include "traffic-guru.repo.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: repo
        image: {{ include "traffic-guru.pipy.image" . }}
        imagePullPolicy: {{ .Values.tg.image.pullPolicy }}
        ports:
        - name: repo
          containerPort: {{ .Values.tg.services.repo.containerPort }}
        command:
        - sh
        - -c
        - |
          /usr/local/bin/pipy --admin-port={{ .Values.tg.services.repo.port }}
        resources:
          {{- toYaml .Values.tg.repo.resources | nindent 10 }}
        livenessProbe:
          periodSeconds: 20
          tcpSocket:
            port: {{ .Values.tg.services.repo.port }}
        readinessProbe:
          periodSeconds: 20
          tcpSocket:
            port: {{ .Values.tg.services.repo.port }}
        startupProbe:
          failureThreshold: 10
          initialDelaySeconds: 5
          periodSeconds: 10
          tcpSocket:
            port: {{ .Values.tg.services.repo.port }}
      priorityClassName: system-node-critical
      terminationGracePeriodSeconds: 30
      serviceAccountName: {{ include "traffic-guru.serviceAccountName" . }}
      {{- with .Values.tg.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tg.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.tg.affinity.enabled }}
      affinity:
        {{- with .Values.tg.affinity.nodeAffinity }}
        nodeAffinity:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: flomesh.io/app
                  operator: In
                  values:
                  - {{ printf "%s-%s" .Chart.Name .Values.tg.repo.name}}
              topologyKey: kubernetes.io/hostname
            weight: 100
      {{- end }}
      {{- with .Values.tg.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}