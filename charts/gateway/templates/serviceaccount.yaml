{{- if and .Values.fsm.ingress.enabled (not .Values.fsm.gatewayApi.enabled ) (semverCompare ">=1.21-0" .Capabilities.KubeVersion.GitVersion) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fsm.gateway.serviceAccountName" . }}
  namespace: {{ .Values.gw.namespace }}
  labels:
    {{- include "fsm.labels" . | nindent 4 }}
  {{- with .Values.fsm.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}